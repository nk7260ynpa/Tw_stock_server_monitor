services:
  tw-stock-monitor:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: tw-stock-server-monitor
    volumes:
      - ../logs:/app/logs
    restart: unless-stopped
